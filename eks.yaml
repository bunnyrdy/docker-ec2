apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: expense
  region: us-east-1

managedNodeGroups:
  - name: expense
    instanceTypes:      
      - t3.small
      - t3a.small
    desiredCapacity: 1
    minSize: 1
    maxSize: 2
    spot: true