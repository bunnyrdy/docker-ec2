apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: expense
  region: us-west-2

managedNodeGroups:
  - name: expense
    instanceTypes:
      - t3.medium
      - t3.large
      - m5.large
    desiredCapacity: 1
    minSize: 1
    maxSize: 2
    spot: true